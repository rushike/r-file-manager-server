import {expect} from "chai"


describe('file-manager', ()=>{
    it("should return all top level files in  chonky format in './data/test'", ()=>{
        try {
            expect(1).to.equal(1);
        }catch(err){
            expect(1).to.equal(0);
        }
    })
})